HONEYPOT_TOKEN=hpt_paste_the_full_secret_from_create_or_rotate
# everything else is optional — API is https://jarnis.io/api
# container listens 22 / 23 / 8080
SSH_HOST_PORT=22
TELNET_HOST_PORT=23
HTTP_HOST_PORT=8080
